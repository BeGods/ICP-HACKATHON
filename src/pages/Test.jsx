import React from "react";
import Footer from "../components/Footer";

const Test = ({ value }) => {
  return (
    <div className="flex h-screen w-screen">
      <Footer />
    </div>
  );
};

export default Test;
